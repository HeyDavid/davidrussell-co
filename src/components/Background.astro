---
---

<section class="card reveal" id="background" aria-labelledby="background-title">
  <div class="section-head">
    <span class="section-num" aria-hidden="true">4.</span>
    <span class="section-label">Background</span>
  </div>
  <h2 id="background-title">
    A decade of marketing, two ventures with my name on the door, and
    a lot of late nights shipping.
  </h2>

  <div class="bg-finds">
    <div class="bg-find">
      <div class="find-name">Marketing operator · 10+ years</div>
      <div class="find-why">Paid, content, brand, productized — most channels, most stages.</div>
    </div>
    <div class="bg-find">
      <div class="find-name">Moolaguides · founder &amp; exit</div>
      <div class="find-why">Built it, grew it to $400K+, sold it. <a href="https://www.collegemagazine.com/making-moola-cm-interviews-the-moolaguides-team/" target="_blank" rel="noopener">Covered in College Magazine</a>.</div>
    </div>
    <div class="bg-find">
      <div class="find-name">Syllble · co-founder</div>
      <div class="find-why">Collaborative fiction publishing. 105+ novels and counting.</div>
    </div>
    <div class="bg-find">
      <div class="find-name">Let's Chat · current</div>
      <div class="find-why">Productized agency for service businesses. Websites, AI, the parts in between.</div>
    </div>
    <div class="bg-find">
      <div class="find-name">Maker Margin · current</div>
      <div class="find-why">Free fee calculator for Etsy sellers. Quietly compounding.</div>
    </div>
    <div class="bg-find">
      <div class="find-name">Magic · 20+ years</div>
      <div class="find-why">Card and coin work since I was a kid. Still the best icebreaker I've got.</div>
    </div>
  </div>

  <p class="bg-footer-note">
    More detail on <a href="https://www.linkedin.com/in/heydavidr/" target="_blank" rel="noopener">LinkedIn</a>.
    Tools and books I actually use are on the
    <a href="/recommended/">recommended</a> page.
  </p>
</section>

<style>
  .bg-finds {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0 32px;
  }
  .bg-find {
    padding: 14px 0;
    border-bottom: 1px dotted var(--border);
  }
  .bg-find:last-child,
  .bg-find:nth-last-child(2):nth-child(odd) { border-bottom: none; }
  .find-name {
    font-size: 16px;
    font-weight: 500;
    color: var(--ink);
    margin-bottom: 3px;
  }
  .find-why {
    font-size: 14px;
    color: var(--ink-soft);
    line-height: 1.50;
  }
  .find-why a {
    color: var(--ink-soft);
    border-bottom: 1px solid rgba(199, 93, 60, 0.25);
  }
  .find-why a:hover { color: var(--accent); border-bottom-color: var(--accent); }
  .bg-footer-note {
    margin-top: 28px;
    font-size: 14px;
    color: var(--ink-soft);
  }
  .bg-footer-note a {
    color: var(--ink);
    border-bottom: 1px solid var(--border);
  }
  .bg-footer-note a:hover { color: var(--accent); border-bottom-color: var(--accent); }

  @media (max-width: 640px) {
    .bg-finds { grid-template-columns: 1fr; }
    .bg-find:last-child { border-bottom: none; }
    .bg-find:nth-last-child(2):nth-child(odd) { border-bottom: 1px dotted var(--border); }
  }
</style>
