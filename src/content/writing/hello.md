---
title: "Hello, this is where I'll write"
description: "A placeholder post so the blog system has something to render. Replace this with the first real essay."
publishDate: 2026-05-10
readTime: "1 min"
draft: true
---

This is the first post on the new blog. It's marked as a draft, so it won't show up on the homepage or the writing index when the site is built for production.

When the first real essay is ready, drop a new markdown file in `src/content/writing/` with frontmatter like this, set `draft: false`, and it'll show up automatically — no code changes, no rebuilds beyond the normal deploy.

## Why markdown

Writing in markdown means the content lives in plain text files in the repo. That's good for two reasons:

1. **Version control.** Every edit is a commit. Rollback is trivial.
2. **Speed.** No CMS, no database, no admin UI. Just write and ship.

## What's next

The next post here will be the first real essay — operator notes, a teardown, or something built. Subscribe up top to get it when it's live.
